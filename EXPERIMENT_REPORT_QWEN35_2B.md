# Qwen3.5-2B 汉字 32×32 点阵冻结线性探针实验报告

## 1. 摘要

本实验研究：冻结 `Qwen/Qwen3.5-2B-Base` 的全部预训练参数后，单个汉字的隐藏状态能否通过一个线性输出头恢复为 32×32 二值字形。

实验使用 5,500 个互不重复、被 Noto Sans CJK SC 支持且被 Qwen3.5 tokenizer 编码为单 Token 的汉字。数据按字符随机划分为训练 3,850 字、验证 825 字、测试 825 字，同一个汉字不会跨集合出现。模型骨干全程冻结，只训练 `Linear(2048, 1024)`。

结论分为两部分：

1. 最后层隐藏状态确实包含显著的字符判别/字形身份信号。测试集最近字形检索 Top-1 为 7.03%，显著高于打乱标签和随机机会的 0.12%。
2. 该信号尚不足以忠实重建未见汉字。最后层前景 F1 为 0.4598，低于平均字形基线的 0.5804，825 个测试字的完整 1024 位匹配率为 0。

输入 Embedding 的检索 Top-1 达到 19.76%，明显高于最后层的 7.03%。这说明字形身份信息更多存在于词元嵌入中，经过模型层后有所减弱，但没有完全消失。

## 2. 研究问题与判定标准

本轮实验回答三个问题：

1. 冻结模型的最后层隐藏状态能否在线性映射下跨未见字符泛化？
2. 可恢复信息是否超过“输出所有汉字的平均形状”与“随机标签”对照？
3. 字形身份信号主要存在于输入 Embedding，还是经过模型计算后的最后层？

“存在信号”与“能忠实画出汉字”必须分开判断：

- 前景 F1、IoU、Hamming 距离衡量像素重建质量。
- 最近字形检索衡量预测结果是否包含目标字符特异信息。
- Exact Match 要求 1024 个二值点全部正确，是最严格的重建指标。
- 普通像素准确率不能单独作为结论，因为背景像素和汉字共有轮廓会造成虚高。

## 3. 数据

### 3.1 数据源

| 数据 | 用途 | SHA-256 |
|---|---|---|
| Unicode Unihan | 汉字候选集合 | `f7a48b2b545acfaa77b2d607ae28747404ce02baefee16396c5d2d7a8ef344b5e` |
| NotoSansCJKsc-Regular.otf | 确定性字形渲染 | `2c76254f6fc379fddfce0a7e84fb5385bb135d3e399294f6eeb6680d0365b744b` |
| 中文维基百科首个文章分片 | 字频统计 | `6277a5bc5a833a8ec7e9a3d90a81d2eaa9400b52732223094e4bf2f5c595b4f2` |

中文维基分片实际扫描 84,916 个页面，包含 131,950,563 次汉字出现和 16,769 个不同汉字。

### 3.2 字符筛选

筛选条件如下：

1. 字符存在于 Unihan IRG Sources。
2. Noto Sans CJK SC Regular 的 Unicode cmap 包含该字符。
3. Unicode 类别为字母，且属于 Han Script。
4. Qwen3.5 tokenizer 在不添加特殊 Token 时将该字符编码为一个 Token。
5. 按本次中文维基语料中的字频排序。

严格交集内共观察到 5,745 个可用单 Token 汉字，少于原计划的 8,000 字。正式实验取其中前 5,500 字；没有为了凑足 8K 而混入多 Token 字符。

### 3.3 点阵生成

- 画布：32×32 灰度图。
- 字体：Noto Sans CJK SC Regular。
- 字号：30。
- 对齐：依据实际 glyph bounding box 水平、垂直居中。
- 二值阈值：128。
- 数据内容 SHA-256：`d5b49082888a1a453eec98490017333863afae36b68a7a62f1b124ee43323bf9`。
- 平均前景像素比例：28.59%。

### 3.4 防泄漏拆分

随机种子为 `20260830`，按字符身份一次性划分：

| 集合 | 字符数 | 比例 |
|---|---:|---:|
| 训练 | 3,850 | 70% |
| 验证 | 825 | 15% |
| 测试 | 825 | 15% |

测试集所有汉字均未参与输出头训练。模型选择和二值阈值只使用验证集，测试集只用于最终指标。

## 4. 模型与训练方法

### 4.1 冻结骨干

| 项目 | 值 |
|---|---|
| 模型 | `Qwen/Qwen3.5-2B-Base` |
| revision | `b1485b2fa6dfa1287294f269f5fb618e03d52d7c` |
| 模型类 | `Qwen3_5ForCausalLM` |
| hidden size | 2048 |
| 骨干精度 | BF16 |
| Attention | PyTorch SDPA |
| 骨干梯度 | 全部关闭 |
| 输出头 | `Linear(2048, 1024)` |
| 输出头参数量 | 2,098,176 |

对每个单 Token 汉字提取两种特征：

- `last_hidden`：最后层目标位置隐藏状态。
- `input_embedding`：同一 Token 的输入 Embedding。

所有特征离线缓存；训练输出头时语言模型不再驻留显存。特征标准化只使用训练字符计算逐维均值与标准差，然后应用于验证和测试特征。

### 4.2 损失与选择

- 主损失：加权 `BCEWithLogitsLoss + 0.5 × Dice Loss`。
- 优化器：AdamW。
- 学习率：0.001。
- weight decay：0.0001。
- batch size：256。
- 最大 epoch：300。
- early stopping patience：15。
- 二值阈值：只在验证集的 0.05～0.95 网格上按前景 F1 选择。
- 置信区间：按测试字符 bootstrap 1,000 次，报告 95% 区间。

### 4.3 对照实验

1. `last_hidden`：正式主实验。
2. `input_embedding`：判断信号在进入模型层前是否更强。
3. `shuffled_labels`：保留最后层特征，但随机打乱训练字符与字形标签的对应关系。
4. `mean_glyph`：所有测试字都输出训练集的逐像素平均字形。

## 5. 结果

括号为 95% bootstrap 置信区间。检索 gallery 是同一批 825 个测试字，因此随机 Top-1/Top-5 机会水平分别为 0.12% 和 0.61%。

| 方法 | Precision | Recall | 前景 F1 | IoU | 像素准确率 | Exact | Hamming | 检索 Top-1 | 检索 Top-5 |
|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| last_hidden | 0.3997 | 0.5412 | 0.4598 [0.4533, 0.4665] | 0.2985 [0.2931, 0.3042] | 0.6387 | 0 | 0.3613 [0.3563, 0.3669] | 0.0703 [0.0533, 0.0885] | 0.1709 [0.1442, 0.1952] |
| input_embedding | 0.4361 | 0.5935 | 0.5028 [0.4961, 0.5089] | 0.3358 [0.3299, 0.3413] | 0.6665 | 0 | 0.3335 [0.3289, 0.3385] | 0.1976 [0.1697, 0.2242] | 0.4012 [0.3685, 0.4315] |
| shuffled_labels | 0.2841 | 1.0000 | 0.4425 [0.4385, 0.4461] | 0.2841 [0.2808, 0.2871] | 0.2841 | 0 | 0.7159 [0.7129, 0.7192] | 0.0012 [0.0000, 0.0037] | 0.0061 [0.0012, 0.0121] |
| mean_glyph | 0.4242 | 0.9185 | 0.5804 [0.5759, 0.5846] | 0.4088 [0.4044, 0.4130] | 0.6226 | 0 | 0.3774 [0.3749, 0.3802] | 0.0012 [0.0000, 0.0036] | 0.0061 [0.0012, 0.0121] |

### 5.1 验证集选择结果

| 方法 | 阈值 | 最佳 epoch | 验证集 F1 | 可训练参数 |
|---|---:|---:|---:|---:|
| last_hidden | 0.55 | 10 | 0.4656 | 2,098,176 |
| input_embedding | 0.55 | 10 | 0.5039 | 2,098,176 |
| shuffled_labels | 0.05 | 2 | 0.4437 | 2,098,176 |
| mean_glyph | 0.30 | 0 | 0.5827 | 0 |

### 5.2 配对前景 F1 差值

正值表示前一个方法更好。区间使用同一批测试字符做配对 bootstrap。

| 对比 | F1 差值 | 95% CI | P(差值 > 0) |
|---|---:|---:|---:|
| last_hidden − shuffled_labels | +0.0173 | [+0.0119, +0.0232] | 1.000 |
| last_hidden − mean_glyph | -0.1206 | [-0.1263, -0.1150] | 0.000 |
| last_hidden − input_embedding | -0.0430 | [-0.0485, -0.0374] | 0.000 |
| input_embedding − mean_glyph | -0.0776 | [-0.0825, -0.0725] | 0.000 |
| input_embedding − shuffled_labels | +0.0603 | [+0.0553, +0.0652] | 1.000 |

### 5.3 字频分桶

测试集按本次维基字频三等分，每桶 275 字。

| 方法 | 字频桶 | 前景 F1 | IoU | Hamming |
|---|---|---:|---:|---:|
| last_hidden | 低频 | 0.4835 | 0.3188 | 0.3540 |
| last_hidden | 中频 | 0.4637 | 0.3019 | 0.3715 |
| last_hidden | 高频 | 0.4296 | 0.2735 | 0.3585 |
| input_embedding | 低频 | 0.5145 | 0.3464 | 0.3398 |
| input_embedding | 中频 | 0.5082 | 0.3407 | 0.3427 |
| input_embedding | 高频 | 0.4832 | 0.3186 | 0.3181 |
| shuffled_labels | 低频 | 0.4499 | 0.2902 | 0.7098 |
| shuffled_labels | 中频 | 0.4532 | 0.2930 | 0.7070 |
| shuffled_labels | 高频 | 0.4241 | 0.2691 | 0.7309 |
| mean_glyph | 低频 | 0.5877 | 0.4161 | 0.3733 |
| mean_glyph | 中频 | 0.5925 | 0.4209 | 0.3701 |
| mean_glyph | 高频 | 0.5604 | 0.3893 | 0.3887 |

## 6. 分析

### 6.1 最后层存在字符特异信号，但不是高质量重建

`last_hidden` 的检索 Top-1 为 7.03%，其置信区间下界 5.33% 仍远高于随机机会 0.12% 和打乱标签对照。这证明输出并非只是在画一个通用汉字轮廓；预测点阵保留了可用于区分目标字形的结构。

但是，前景 F1 只有 0.4598，Exact Match 为 0。当前结果不能支持“只替换输出层即可准确输出 32×32 汉字点阵”这一强结论。

### 6.2 输入 Embedding 比最后层更强

输入 Embedding 的 Top-1/Top-5 为 19.76%/40.12%，最后层只有 7.03%/17.09%；Embedding 的 F1 和 Hamming 也更好。这说明 Qwen3.5 的 Token Embedding 中存在较强的字符身份信息，而最后层为语言建模目标进行变换后，这种线性可恢复的几何信息有所衰减。

这不等同于证明 Embedding 直接编码了部首或笔画。Token 身份本身也可以与固定字形形成统计映射，后续仍需用 IDS 构件不重叠拆分区分“字符查表结构”与“部件组合泛化”。

### 6.3 平均字形说明 F1 不能单独使用

平均字形基线通过覆盖大多数汉字共享的居中方块与常见笔画区域，取得 0.9185 Recall 和最高的 0.5804 F1，但检索准确率恰好处于随机水平。它并不知道目标是什么字。

因此，本项目必须同时报告前景重建指标和最近字形检索。只报告 F1 会错误地把“通用汉字形状模板”判断为优于包含目标身份信息的预测。

### 6.4 字频结果需谨慎解释

本轮低频和中频桶的 F1 没有低于高频桶，甚至略高。可能原因包括不同字频桶的平均笔画密度和复杂度不同，也可能是 tokenizer 的单 Token 筛选造成选择偏差。在加入笔画数、部首和 IDS 结构元数据前，不能把该结果解释为模型对生僻字泛化更好。

## 7. 结论

本轮得到的是“部分支持、强重建目标不成立”的结果：

- 支持：冻结 Qwen3.5-2B 的输入 Embedding 和最后层隐藏状态中都存在可被线性头读出的字符特异信号，并能泛化到没有参与输出头训练的字符。
- 不支持：最后层线性输出头尚不能忠实恢复 32×32 二值汉字；测试集没有任何 Exact Match，F1 也没有超过平均字形模板。
- 新发现：输入 Embedding 比最后层更容易解码，说明研究下一步应做分层探针，而不是只测试最后一层。
- 方法学发现：平均字形能获得很高的 F1，却没有任何字符识别能力；最近字形检索是不可省略的指标。

## 8. 限制

1. 本轮只运行 Qwen3.5-2B-Base，不能直接外推到 4B 主模型或 Qwen3 文本对照。
2. 拆分按字符身份随机进行，还没有基于 IDS 做“部件已见、组合未见”的严格拆分。
3. 输入就是目标汉字本身，尚未测试“纯中文上下文预测下一个汉字点阵”。
4. 只使用一种字体和一个二值阈值，未测试跨字体泛化。
5. 最近字形 gallery 只包含 825 个测试字符，不是完整 Unicode 汉字集合。
6. 本轮只能证明线性可读的字符特异信息，不能单独证明模型内部显式表示了象形结构。

## 9. 下一步实验

优先级建议如下：

1. 在 Embedding、25%、50%、75% 和最后层训练相同探针，定位字形信号随深度的变化。
2. 下载并运行 Qwen3.5-4B-Base 与 Qwen3-4B-Base，在两个 tokenizer 的单 Token 交集上比较。
3. 引入 CHISE IDS，建立部件组合不重叠测试集，区分字符身份信息与真正的构件泛化。
4. 在中文维基上下文上构建“前缀 → 下一汉字点阵”任务，目标字不出现在输入中。
5. 加入第二字体作为域外测试，确认探针恢复的是字形结构而不是固定字体像素模板。

## 10. 环境、产物与复现

### 10.1 环境

| 项目 | 值 |
|---|---|
| 实验代码提交 | `9b4ebfa237291c12ab8c2de45e33fdf2a250ab88` |
| GPU | NVIDIA GeForce RTX 5060 Ti 16GB |
| Python | 3.12.13 |
| PyTorch | 2.11.0+cu128 |
| Transformers | 5.16.1 |
| 特征提取峰值显存 | 4.820 GiB |
| 实验完成时间 | 2026-08-30 14:36（Asia/Shanghai） |

### 10.2 服务器产物

完整产物位于服务器：

```text
/root/HansGPT/artifacts/experiments/qwen35_2b_hanziglyph_5k/
├── REPORT.md
├── metrics.json
├── predictions.png
├── resolved_config.json
├── dataset/
├── features/
└── probes/
```

其中包含数据清单、特征缓存、训练曲线、每个探针的权重、逐方法指标和预测图；这些大文件受 `.gitignore` 保护，不提交到公开仓库。

### 10.3 复现命令

在训练服务器执行：

```bash
cd /root/HansGPT
git pull --ff-only origin main
uv sync --frozen
uv run ruff check .
uv run pytest -q
uv run hansgpt-glyph-probe \
  --config configs/experiments/qwen35_2b_hanziglyph_5k.json
```

本实验运行前后服务器源码仓库均保持干净；模型、数据、缓存、权重和报告产物只存在服务器忽略目录中。
