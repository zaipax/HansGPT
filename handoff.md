# HansGPT Handoff

## Current state

The active research direction is the pure Transformer C architecture: 4×4 patch self-attention glyph encoder, 24-layer width-1024 GQA GPT backbone (16Q/4KV, FFN 2816), and a 4-layer width-256 causal byte Transformer (8 heads, FFN 768) that autoregressively emits 128 bytes and reconstructs one 32×32 binary glyph. Actual baseline size is 275,349,504 parameters. Full design and prior ABC results are in `docs/04_architecture_exploration.md` and `reports/attention_abc_r2/REPORT.md`.

## Active training

- GPU3 guarded parallel-byte continuation was launched on 2026-09-17 from commit `b63dd2e`, resuming the original 50M checkpoint to 100M. Config: `configs/experiments/hansgpt_qwen3_parallel_byte_gpu3_100m_recovery.json`. Tmux: `parallel-byte-gpu3-recovery`; console: `artifacts/logs/parallel_byte_gpu3_recovery.console.log`; status: `artifacts/logs/hansgpt_qwen3_parallel_byte_gpu3_100m_recovery_v2_full/status.json`. Nonfinite FP16 forward values trigger same-batch eager FP32 recomputation with unchanged cursor/mask/RNG and cleared partial gradients. If FP32 also fails, preserve batch and a diagnostic-only state snapshot; those snapshots cannot be normally resumed. Checkpoint every 10M, keep latest and 100M final, preserving the original 50M source. Successful completion automatically evaluates into `artifacts/reports/parallel_byte_gpu3_100m_recovery_inference_v2/`. Thirty focused checks passed; real 1.5B FP32 smoke completed 65,536 additional positions in 17 updates, no new overflow, peak allocation 26.46 GiB. Historical 91.57M failure cause remains unproven because its weights were lost; exact batch replay at 50M was finite in eager/compiled FP16 and FP32. See `reports/parallel_byte_10m/NONFINITE_RECOVERY.md` and the prior `RESULT_50M.md`.

- The Qwen3-style C 1.5B full-corpus run on physical GPUs 4–7 completed on 2026-09-17 from commit `27c705d`. The configuration was `configs/experiments/hansgpt_qwen3_c_1p5b_gpu4_7_full_corpus.json`: per-rank batch 4 (global batch 16), context 1024, head chunk 1024, FP16, backbone recomputation, xFormers and fused AdamW.
- The LR is `2e-4` peak with a 20,000,000-target linear warmup, then global cosine to `2e-5` over the verified 1,089,139,385-target corpus horizon. Checkpoints are written every 100,000,000 targets and retained at those milestones plus the recent retention window.
- The run is in tmux session `qwen3-1p5b-4567` on the training server, with console log `artifacts/logs/qwen3_1p5b_gpu4_7_full.console.log` and status under `artifacts/logs/hansgpt_qwen3_c_1p5b_gpu4_7_full_corpus_v1_full/status.json`. At the latest check on 2026-09-15 after the inference benchmark, it was still training normally at 276,027,147 targets and 16,965 updates with zero AMP overflows; physical GPUs 4-7 were at full utilization while GPUs 0-3 were idle. Peak device use is about 28.8 GiB per training card.
- The required PCIe fallback variables are `NCCL_P2P_DISABLE=1` and `NCCL_CUMEM_HOST_ENABLE=0`. `uv sync --frozen` removed the undeclared xFormers package, so the server project environment restored `xformers==0.0.32.post2` with `uv` before launch.
- Completion correction: the 4–7 run finished on 2026-09-17 at 1,089,139,385 targets and 66,944 updates, with 19 AMP retries. Final validation NLL was 0.00235677 nats/pixel; final checkpoint is `artifacts/checkpoints/hansgpt_qwen3_c_1p5b_gpu4_7_full_corpus_v1_full/positions_1089139385.pt`. The older “still training” sentence above is historical.

## Experiments completed

- Final 1.089B checkpoint effect evaluation completed on GPU0 on 2026-09-18. The 32-window validation subset (5,517 targets) had NLL 0.00235677 nats/pixel (2.41333 nats/grid). On 64 teacher-forced held-out positions, foreground F1 was 0.6441, IoU 0.4750, exact bitmap match 50.0%, and top-1 gallery retrieval 50.0%; shuffling contexts raised NLL from 0.002496 to 0.008095 on 61/64 positions. Eight 128-grid greedy generations had 99.88% exact content membership, no adjacent exact repeats, EOS in 2/8, but phrase-cycle flags in 5/8; outputs visibly repeat phrases such as “一天一个” and “不可取代的权利”. Eight sequential temperature-1.0 samples of 64 grids had 98.75% exact content membership, EOS in 3/8, no adjacent repeats or detected short/phrase cycles, but semantic coherence remained weak. Contact sheets and JSON reports are under ignored `artifacts/reports/qwen3_c_1p5b_final_eval_v1/` and `artifacts/reports/qwen3_c_1p5b_final_sampled_v1/`. This confirms normal binary glyph rendering and strong glyph-inventory legality, while long-form language quality and repetition remain unresolved.

- GPU2 single-GPU parallel-byte speed ablations completed on 2026-09-18 local time. At batch 4/context 1024, fixed-window baseline was 3336 positions/s; chunk 2048 was 3350 (+0.41%, within repeat variation), disabling backbone checkpointing OOMed, and packed CPU preprocessing was 3335 (no end-to-end gain despite 2.36x faster CPU collation). Whole-backbone compilation hit Torch 2.8 CUTLASS meta-schema and HF wrapper errors. An opt-in first-order CUTLASS backward compiler boundary plus per-layer forward compilation produced 4076 (+22.19%). Distinct-window 10-warmup/40-measured confirmation at `e4b1b74`: baseline 3349.51, compiled 4066.19 (+21.40%), compiled+chunk2048+packed 4085.89 (+21.98%) positions/s. Compiled peak allocated memory 24.111 GiB vs baseline 24.371 GiB; finite updates, maximum paired NLL difference about 1e-5. Prefer compiled blocks with checkpointing/chunk1024; combined extra 0.48% is not established beyond noise. No active training configuration was changed. Reproduction flags: `--compile-layers --opaque-backward` in `scripts/benchmark_parallel_single_gpu.py`, physical GPU2 only. Full report: `reports/parallel_byte_10m/SINGLE_GPU_SPEED.md`; raw server reports: `artifacts/reports/single_gpu2_speed_v1/`. The compiler boundary reuses CUTLASS and is not handwritten CUDA. These are short random-initialization throughput tests, not long-run convergence or glyph-quality evidence.

- Storage cleanup on 2026-09-17: with explicit user approval, removed only serial GPU4-7 checkpoints at 300M/400M/500M/600M/700M (about 85 GiB). Retained serial 100M/200M/800M/900M/1B, all logs/reports and active training. Removed completed disposable parallel smoke checkpoints. Space recovered from 3.7 GiB to about 106 GiB before the new continuation.

- GPU3 parallel-byte 10M pilot completed at commit `0c58011`: 10,000,000 valid positions, 2,458 steps, zero AMP overflows, 3,122.43 positions/s, 25.15 GiB peak allocation. Validation NLL was 0.22115571 nats/pixel. All nine broad inference conditions had zero exact content inventory membership and zero EOS; inspected greedy PNGs showed fragmented horizontal strokes and blanks. This establishes failure at 10M but does not prove an inherent architectural limitation. Checkpoint `artifacts/checkpoints/hansgpt_qwen3_parallel_byte_gpu3_10m_v1_full/positions_010000000.pt` is retained; SHA256 `b13594f068d912a0a4897dc0a4d9fd467ab2fcd221bff24aa4af26c26efada99`. Reports and raw PNGs/NPZs: `artifacts/reports/parallel_byte_gpu3_10m_inference_v1/`. Full protocol: `reports/parallel_byte_10m/PROTOCOL.md`.

- CVAE learning-rate search: 8 GPUs, bsz10, ctx1024, chunk2048, 10M Han each. Best validation convergence was peak LR `3e-4`; high LR `8e-4` was poor. This was posterior reconstruction, not fluent generation.
- Four-GPU CVAE 100M positions: global bsz40, peak LR `3e-4`; reconstruction improved but prior generation remained weak.
- C byte model, single GPU: ctx1024, bsz10, chunk2048, no recomputation: about 7,480 positions/s, ~28.9 GiB allocated.
- C byte model, four GPUs 4–7: bsz8/rank, chunk2048, 10M positions: 20,640 positions/s; raw greedy output initially collapsed to blank/comma glyphs.
- C byte model, eight GPUs 0–7: bsz8/rank, chunk2048, full-corpus LR schedule, 100M positions: 37,630 positions/s. Validation NLL reached 0.01034 nats/pixel, but greedy generation produced repeated commas. Sampling produced varied but incoherent text.
- Four-GPU full-corpus run on GPUs 4–7 used bsz8/rank, ctx1024, chunk2048, no recomputation, one complete dataset pass target (1,089,139,385 valid positions), global cosine schedule, peak LR3e-4, warmup10,891,394, and final LR3e-5. It stopped near completion after reaching 1.08B positions; inspect `artifacts/logs/hansgpt_byte_c_four_gpu_b8_h2048_full_corpus_v1_full/status.json` before acting. All saved checkpoint files were deleted on 2026-09-14 to reclaim server disk; logs and reports remain.
- Single-GPU memory probes with chunk128 and recomputation showed safe scaling through 1.42B parameters (~23.7 GiB device use); 1.79B used ~29.1 GiB and is an aggressive ceiling. Chunk128 is useful for capacity probing; chunk2048 is faster but near the limit for large models.
- Qwen3-style C-Qwen1.5B benchmark completed on physical GPUs 0-3 at commit `08210da`: hidden 2048, 30 layers, 16Q/8KV, head dim 128, FFN 6144, QK-Norm before RoPE, RoPE theta 1e6, and 1,515,243,008 parameters. With FP16, gradient checkpointing, xFormers, fused AdamW, context 1024, head chunk 128, and batch 1 per rank, measured throughput was 1,377 valid glyph targets/s globally; per-rank peak allocated memory was 22.81 GiB (reserved 23.38 GiB). The benchmark used real `chinese_document_v3` windows and selected only GPUs 0-3; the active 4-7 run remained at roughly 29 GiB per card.
- Qwen3-style C-Qwen1.5B single-GPU diagnostic on GPU0 at commit `5a6e540`: the same context, batch, head chunk, xFormers, FP16, fused AdamW, and 10-step warmup measured 2,041.6 valid glyph targets/s with backbone recomputation enabled (four measured steps, 0.494-0.498 s/step, peak allocated 23.19 GiB). With recomputation disabled, the measured steps were 0.427-0.429 s (about 2.34-2.39k targets/s) and peak device use was 27.36 GiB; its final checkpoint could not be written because the server filesystem was full, but the throughput measurements completed.
- Qwen3-style C-Qwen1.5B single-GPU benchmark on physical GPU7 at commit `e8217ba`: with backbone recomputation enabled and the same batch 1, context 1024, head chunk 128, xFormers, FP16, fused AdamW, and 10-step warmup, four measured steps processed 4,077 valid targets in 1.9928 seconds, or 2,045.86 targets/s. Steps took 0.4951-0.4979 seconds; peak allocated/reserved memory was 23.19/23.88 GiB and no AMP overflow occurred. This is 0.21% faster than the prior GPU0 result, confirming normal single-GPU compute throughput. The temporary checkpoint was deleted; the persistent report is `artifacts/reports/hansgpt_qwen3_c_1p5b_gpu7_single_v1_full/result.json`.
- The same Qwen3-style C-Qwen1.5B benchmark on physical GPU7 with batch 2 also fit with backbone recomputation enabled: four measured steps processed 8,146 valid targets in 3.4277 seconds, or 2,376.55 targets/s. Steps took 0.8261-0.8819 seconds; peak allocated/reserved memory was 23.86/24.55 GiB, peak device use was 24.94 GiB, and no AMP overflow occurred. This is 16.2% more throughput than batch 1 while using about 0.67 GiB more reserved memory. The temporary checkpoint was deleted; the persistent report is `artifacts/reports/hansgpt_qwen3_c_1p5b_gpu7_single_b2_v1_full/result.json`.
- The batch-4 steady-state follow-up on physical GPU7 at commit `e8217ba` used the same recomputation-on configuration, 10 unmeasured warmup steps, and 20 measured steps. It processed 81,355 valid targets in 30.6284 seconds, or 2,656.19 targets/s. Measured steps took 1.5036-1.6088 seconds (mean 1.5211, median 1.5161); excluding the first measured step gives 2,682.66 targets/s, so startup overhead does not materially affect the result. Peak allocated/reserved/device memory was 24.66/25.38/25.77 GiB and no AMP overflow occurred. Throughput improved 29.8% over batch 1 and 11.8% over batch 2. The temporary checkpoint was deleted; the persistent report is `artifacts/reports/hansgpt_qwen3_c_1p5b_gpu7_single_b4_steady_v1_full/result.json`.
- A GPU7 head-chunk sweep at batch 4, context 1024, backbone recomputation enabled, 10 warmup steps and 20 measured steps used xFormers `0.0.32.post2` with the same Qwen3 1.5B model. Head chunk 256 completed at 2,703.41 targets/s (2,720.55 excluding the first measured step), with mean/median step time 1.4958/1.4951 s and peak allocated/reserved memory 24.66/25.38 GiB. Chunk 512 completed at 2,718.84 targets/s (2,736.66 excluding the first step), mean/median 1.4867/1.4806 s, and peak allocated/reserved 24.66/25.31 GiB. Chunk 1024 completed at 2,742.48 targets/s (2,764.51 excluding the first step), mean/median 1.4720/1.4723 s, and peak allocated/reserved 24.67/27.26 GiB. All successful runs processed 81,355 targets with zero AMP overflows. Chunk 2048 OOMed during warmup step 2 while allocating an additional 384 MiB (only 68 MiB free; 31.66 GiB already in use), so it has no valid throughput result. The persistent reports/status are under `artifacts/reports/` and `artifacts/logs/` with names `hansgpt_qwen3_c_1p5b_gpu7_b4_h{256,512,1024,2048}_steady_v1_full`; all temporary checkpoints were deleted. Chunk 1024 is the fastest tested safe setting, while chunk 2048 is above the batch-4 memory limit.
- The first GPU4-7 Qwen3 1.5B batch-4/chunk-1024 launch omitted the host's required NCCL fallback variables and remained in CUDA/NCCL initialization for over 12 minutes with no report. Its ranks showed repeated CUDA ioctl and socket polling on the PCIe/NUMA topology. Repeating the run with `NCCL_P2P_DISABLE=1` and `NCCL_CUMEM_HOST_ENABLE=0` let all ranks pass initialization and complete normally; these variables are now part of the reproduction command.
- The successful GPU4-7 Qwen3 1.5B batch-4/chunk-1024 multi-GPU benchmark at commit `e8217ba` used 10 warmup and 20 measured steps, a 256 MiB FP32 gradient bucket, and xFormers `0.0.32.post2`. With physical GPUs `4,5,6,7` and global batch 16, it processed 16,384 targets/update in 78.8246 measured seconds, or **4,157.08 global targets/s** (4,156.53 after dropping the first measured step). Step times were 3.9278-4.1175 s (mean 3.9412, median 3.9313). Mean gradient synchronization took 2.4514 s/update, 62.2% of the step. Per-rank peak allocated/reserved memory was at most 23.64/26.88 GiB, with zero benchmark errors; all GPUs were released after completion. The persistent report is `artifacts/reports/hansgpt_qwen3_c_1p5b_gpu4_7_b4_h1024_steady_v1.json` and the console log is `artifacts/logs/hansgpt_qwen3_c_1p5b_gpu4_7_b4_h1024_steady_v1.run.log`; no checkpoint was written.
- A two-GPU Qwen3 1.5B batch-4/chunk-1024 benchmark ran on physical GPUs `0,1` at commit `01c8f84`, using 10 warmup and 20 measured steps with the same FP16, recomputation, xFormers and 256 MiB bucket settings. Global batch 8 processed 8,192 targets/update in 66.8434 seconds, or **2,451.10 global targets/s**. Mean gradient synchronization was 1.8509 s/update, 55.4% of the 3.3422 s step; per-rank peak allocated/reserved memory was 23.63/26.88 GiB, with zero errors. This is below the single-GPU batch-4/chunk-1024 result (2,742.48 targets/s) and is 59.0% of the four-GPU global result (4,157.08 targets/s), so two cards are not faster for this PCIe-bound workload. The report is `artifacts/reports/hansgpt_qwen3_c_1p5b_gpu0_1_b4_h1024_v1.json` and the console log is `artifacts/logs/hansgpt_qwen3_c_1p5b_gpu0_1_b4_h1024_v1.console.log`.
- The four-GPU result is communication-bound: 4,096 global targets took 2.970-2.978 s/update versus about 0.495 s for one rank. Relative to ideal replication of the recomputation-on single-GPU result (about 8,166 targets/s), 1,377 targets/s is 16.9% scaling efficiency. The model has 1,515,243,008 FP32 gradients (about 5.64 GiB); the benchmark's 8,388,608-element FP32 scratch buffer is 32 MiB, so each update performs roughly 181 sequential NCCL all-reduces. GPUs 0-3 have PCIe PIX/NODE links with no NVLink, making synchronization the dominant cost.
- The GPU4-7 full-corpus C run reached 1,080,000,000 of 1,089,139,385 target positions (33,293 updates) and then stopped at `2026-09-14T06:54:12Z` with `Insufficient space for retained checkpoint`; all GPUs are now idle. Its checkpoint files were later deleted during disk cleanup; logs remain.
- Evaluation of the former checkpoint `positions_1070000000.pt` (1.07B targets, 32 fixed validation chunks, 4,261 targets) completed on GPU0 before cleanup. Teacher-forced greedy byte decoding reached NLL `0.002565` nats/pixel (`2.6269` nats/grid), foreground F1 `0.6481`, IoU `0.4794`, Dice `0.6481`, exact bitmap match `0.4605`, and pixel accuracy `0.8828`. A fixed 32-glyph free-running diagnostic had 100% exact content membership, zero adjacent repeats, and 22 unique glyph bitmaps; eight independent 16-glyph prompts produced 242 body glyphs with 100% exact content membership and 2.56% adjacent repeats. The model no longer falls into the 100M checkpoint's all-comma cycle, but samples still contain phrase-level repetition and only 1/8 prompts emitted EOS within 32 glyphs. Persistent report: `artifacts/reports/hansgpt_byte_c_four_gpu_b8_h2048_full_corpus_v1_eval_1070m/result.json`.
- Extended the same eight-prompt greedy generation to 128 glyphs. The run produced 800 body glyphs, 100% exact content membership, 3.16% adjacent exact repeats, 4/8 phrase-cycle flags, and EOS in 3/8 prompts. The first few glyphs often follow the prompt, but long continuations repeat local phrases such as `一只只熊` and `一个男生`; images are `actual_inference_128_00.png` and `actual_inference_128_04.png` under the persistent evaluation report directory, with `generation_8prompts_128.json` holding the summary.
- Broad raw inference of the 100M Qwen3-style C 1.5B checkpoint ran on physical GPU0 from commit `27c705d` using eight validation documents, prompt lengths 8/32/128/256/512/1024, 256 generated glyphs, greedy decoding, and categorical byte sampling at temperatures 0.7 and 1.0. All 18 conditions completed with xFormers CUTLASS and peak allocated memory of 8.73-9.83 GiB. Greedy outputs had 100% exact content membership but only about 11-15 unique glyph bitmaps per sample and visible phrase loops; sampling reached roughly 99% legal content at temperature 0.7 and 93-95% at temperature 1.0, with EOS rates varying from 12.5-75%. First-8 exact bitmap match stayed about 3-11%, falling to about 1-4% by 256 glyphs, so the checkpoint is not semantically fluent despite producing valid glyph-shaped assets. The reproducible report and contact sheets are under `artifacts/reports/qwen3_c_1p5b_100m_inference_v1/`.
- The identical broad inference matrix completed on physical GPU0 for `positions_200000000.pt` (SHA256 `db2218392c66258a3c91fd8bbca640797d8ec1e504e11b5907cca232c3314c5c`, 1,515,243,008 parameters). All 18 prompt-length/decoding conditions completed in 2,206.1 seconds with peak allocated memory of 8.73-9.83 GiB, and the concurrent GPU4-7 training job was not interrupted. Greedy content membership was 100%, with EOS in 0-12.5% of prompts; temperature-0.7 sampling achieved 99-100% legal glyphs and 37.5-87.5% EOS, while temperature-1.0 achieved 97-98% legal glyphs and 37.5-87.5% EOS. Greedy first-8 exact bitmap match improved to about 6-16%, but full 256-glyph exact match remained only 1-2%. Contact sheets still show severe phrase loops such as `工程项目` and `不可阻挡的国家`; higher-temperature samples are more varied but less coherent and contain more unknown or incorrect glyphs. This is a measurable improvement over 100M in EOS behavior, glyph validity, and early continuation accuracy, but it is not fluent long-form generation. The server report is under `artifacts/reports/qwen3_c_1p5b_200m_inference_v1/`; the complete Windows copy with 18 PNGs, 18 NPZ files, condition JSON files, and `result.json` is under ignored path `artifacts/qwen3_c_1p5b_200m_inference_v1/`.
- Added `scripts/benchmark_qwen3_inference.py` for reproducible single-GPU raw rollout timing. The timed interval excludes checkpoint loading, transcription, PNG/NPZ writes and report scoring; it uses the 200M checkpoint, FP16, xFormers CUTLASS, prompt lengths 8/512/1024, batch sizes 1/8, 32 generated glyphs, 16 warmup glyphs and three measured repeats. The initial run at commit `8642a40` measured about 2.106-2.142 glyphs/s for batch 1 and 15.99-16.21 aggregate glyphs/s for batch 8 (about 2.00-2.03 per sequence). The report and console log are `artifacts/reports/qwen3_c_1p5b_200m_inference_perf_baseline_v1/result.json` and `artifacts/logs/qwen3_c_1p5b_200m_inference_perf_baseline_v1.console.log`.
- A final rerun at commit `92df483` with the same checkpoint and 16-glyph warmup completed on GPU0 while GPUs 4-7 continued training. Median case results were 1.634/1.641/2.106 aggregate glyphs/s for batch 1 at prompt 8/512/1024, and 12.588/12.543/15.557 aggregate glyphs/s for batch 8 (1.574/1.568/1.945 glyphs/s per sequence). Across repeated runs the observed range was 1.63-2.11 glyphs/s per single stream and 12.54-15.56 aggregate glyphs/s at batch 8. GPU0's V100 automatic SM clock varied between roughly 1,245 and 1,560 MHz with no throttle flags, so the range is real under the concurrent training load. The report and console log are `artifacts/reports/qwen3_c_1p5b_200m_inference_perf_steady_v2/result.json` and `artifacts/logs/qwen3_c_1p5b_200m_inference_perf_steady_v2.console.log`.
- The benchmark profile identifies the bottleneck: the inner conditional byte decoder accounts for 81.6-92.0% of rollout CUDA time, the outer 1.5B backbone accounts for 8.3-18.4%, and unattributed host work is below 0.04%. Each glyph still requires 128 serial byte steps, so batch 8 raises aggregate throughput by about 7.7x while leaving per-sequence latency near the batch-1 value. An experimental removal of repeated cache/input validation was tested in one process against the validated reference at commit `9a12188`; outputs matched exactly, but speed was 0.998x at batch 1 and 0.971x at batch 8. The experiment was reverted in `92df483`; decoder semantics are unchanged. Meaningful future speedups require reducing the 128-step dependency or implementing a preallocated/fused inner decoder (possibly with CUDA graphs), rather than removing the checks alone. The A/B report is retained locally at `artifacts/reports/qwen3_c_1p5b_200m_decoder_paths_ab_v1/result.json`.

## Important diagnosis

The apparent comma/blank failure is primarily optimizer-step starvation and weak conditioning, not proven xFormers corruption. Old C at 100M positions performed 30,494 updates because short ctx256 windows contained ~3,280 valid positions/update. New packed ctx1024/global batch64 performed only 1,546 updates at 100M. Historical old checkpoint at step1505 also produced the same comma loop. Native SDPA, xFormers, cached and uncached decoding matched. Sampling exposes learned modes; greedy currently selects a dominant punctuation mode.

## Immediate goals

1. The evaluated `positions_1070000000.pt` checkpoint was purged; use the persistent evaluation report for historical metrics and retrain before further checkpoint-based evaluation.
2. Compare small global batches (per-rank bsz2 or4) against bsz8 at fixed successful-position prefixes to increase optimizer updates while preserving ctx1024 and the full-data LR horizon.
3. For the chosen batch, continue to at least the update regime where old C became readable (~3,000 updates), then evaluate multiple prompts with greedy and categorical sampling, EOS, repetition, raw glyph validity and context-shuffle sensitivity.
4. Do not select a model from posterior reconstruction alone. Require raw prior generation and semantic/repetition review; keep test data out of LR selection.

## Reproduction and artifacts

Use project `uv` only (`uv sync --frozen --inexact`, `uv run ...`). Source changes follow the local worktree → commit/rebase/merge/push → server pull workflow. Reports, logs and images live under ignored `artifacts/`; any future checkpoints may be pruned only after checking active processes and preserving the active run.

The current server checkout is `/root/HansGPT`. Connection details and the supported proxy workflow are kept only in the local ignored `SERVER_CONNECTIONS.md`; never copy its secrets into this document or public messages. The project-level operational rules in `AGENTS.md` remain authoritative.

## Suggested skills

- `diagnose` for blank/comma collapse, update-count comparisons and decoding regressions.
- `handoff` when transferring this state to another session.
- `visualize` for loss curves and side-by-side raw glyph generation.
- `improve-codebase-architecture` before adding resume or distributed-training abstractions.
